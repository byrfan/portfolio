## To Implement:
    ! implement accurating typing (of header i.e. currently only sending text/html) ? through a similar approach to that of the codes? 
    ? Implement in memory cache of all static files
    - Actually Implement static files
    - Implement Portfolio
